<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.data.dao.JoinDao">
	<!-- 개인회원가입-->
	<insert id="insertUser" parameterType="userDto">
		insert into user values (null,#{email},#{password},#{nickname},#{profileImage},#{phone},#{address},#{gender},#{point},#{year},#{date},#{loginType},now(),#{isAdmin})		
	</insert>
	<!-- 개인회원가입 중복체크 -->
	<select id="getUserIdSearch" parameterType="String" resultType="int">
		select sum(cnt) from (select count(*) cnt from user where email=#{email} union all select count(*) cnt from seller where email=#{email}) as t;
	</select>
</mapper>	